@page "/logout"
@inject ILoginService loginService
@inject NavigationManager navigationManager


<text>Deslogueando....</text>

@code {
    protected async override Task OnInitializedAsync()
    {
        await loginService.Logout();
        navigationManager.NavigateTo("/");
    }
}
